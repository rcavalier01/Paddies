# Paddies
Are you wearing green? Hope so


Current File Status:
Arduino_Receive_Serial is a file to upload to pro micro (currently it is uploaded as is).

Use Arduino Leonardo because for the board because it uses the same ATMEGAU32 chip

sftest.py will send a serial command that receive_serial can parse
